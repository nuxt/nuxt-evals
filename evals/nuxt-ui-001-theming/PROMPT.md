Create a branded Nuxt UI application. Configure custom primary ("indigo") and neutral ("zinc") colors in app.config.ts. On the homepage, display a heading with the brand name, a description paragraph, and two buttons (primary and secondary variants). Use semantic color utilities (like text-primary, bg-muted, text-muted) for custom text and background styling instead of raw Tailwind color classes. Also customize the Button component globally in app.config.ts to use rounded-full styling. Use Nuxt UI components.
