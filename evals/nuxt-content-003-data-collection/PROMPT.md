Create a team members page using Nuxt Content data collections. Define a "team" collection with type "data" (not "page") that reads JSON files. Create at least 3 team member JSON files with name, role, and bio fields. Build a page at /team that queries the collection and displays all team members. Use a proper data collection since these are structured records, not renderable pages.
