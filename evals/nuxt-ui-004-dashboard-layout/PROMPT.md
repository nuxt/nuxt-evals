Create a dashboard layout using Nuxt UI's purpose-built dashboard layout system — don't build a custom sidebar with divs and CSS. The layout needs a root dashboard wrapper component, a collapsible sidebar with navigation items (Home, Users, Settings), and a main content area with a slot for pages. Add a dashboard home page that uses the layout and includes a panel with a top navbar. Use definePageMeta to assign the layout.
