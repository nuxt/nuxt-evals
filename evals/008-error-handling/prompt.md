Create error.vue in app/ that displays error messages using useError() and has a button that calls clearError(). Create homepage at app/pages/index.vue with NuxtErrorBoundary component that has #error slot receiving error prop, a button to trigger an error using createError(), and ability to clear the error locally (error = null). Use Nuxt 4 structure (pages, components, composables, assets, etc. in app/ directory).
