Create a public website using Nuxt UI's built-in layout components for the header, main content wrapper, and footer — don't use raw HTML elements like <header>, <nav>, or <footer>. The header should include a site title, navigation links (Home, Features, Pricing) using Nuxt UI's navigation menu, and a right section with a call-to-action button. Add a mobile menu in the header's body slot with vertical navigation. The footer should have copyright text and social links. Add a homepage.
