Create a page with a user profile card that includes a name and email. Add an actions button that opens a dropdown menu with grouped items: a first group with "Edit" and "Duplicate" actions, and a second group with a destructive "Delete" action styled in red/error color. Each item should have an icon. Add an onSelect handler that logs a message to the console when "Delete" is clicked. Use Nuxt UI components.
